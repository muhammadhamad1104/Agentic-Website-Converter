const express = require('express'); const app = express(); app.use(express.json()); app.use('/users', require('./routes/users')); app.use('/sessions', require('./routes/sessions')); app.use('/aboutus', require('./routes/aboutus')); app.use('/contactus', require('./routes/contactus')); app.use('/health', require('./routes/health')); const port = process.env.PORT || 3000; app.listen(port, () => console.log(`Server running on port ${port}`));